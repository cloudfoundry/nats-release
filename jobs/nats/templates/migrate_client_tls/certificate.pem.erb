<% if p("nats.migrate_client.tls.certificate", false) -%>
<%= p("nats.migrate_client.tls.certificate") %>
<%
  else
    raise "nats.migrate_client.tls.certificate not provided in nats job properties"
  end
-%>
